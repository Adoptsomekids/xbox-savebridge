<Page
    x:Class="SaveBridge.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SaveBridge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="24,24,24,12" Spacing="4">
            <TextBlock Text="SaveBridge" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Dead Island DE — Xbox Connected Storage HTTP bridge" FontSize="14"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Padding="24,0,24,12" Orientation="Horizontal" Spacing="12">
            <Button x:Name="StartButton" Content="Start Server" Click="StartButton_Click" MinWidth="130"/>
            <Button x:Name="StopButton"  Content="Stop Server"  Click="StopButton_Click"  MinWidth="130" IsEnabled="False"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Padding="24,0,24,12" Spacing="4">
            <TextBlock x:Name="StatusText" Text="Status: Stopped"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       FontSize="14" TextWrapping="Wrap"/>
            <TextBlock x:Name="AddressText" Text=""
                       FontFamily="Consolas" FontSize="13" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Padding="24,0,24,24" Spacing="8">
            <TextBlock Text="Log" FontWeight="SemiBold" FontSize="14"/>
            <ScrollViewer x:Name="LogScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="8"
                          Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                <TextBlock x:Name="LogOutput" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
