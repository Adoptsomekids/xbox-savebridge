name: Build APPX (Windows)

on:
  push:
    branches: [ main ]
    paths:
      - 'SaveBridge/**'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  release:
    types: [ created ]

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      # Restore NuGet packages (SDK-style, no packages.config)
      - name: Restore
        shell: powershell
        run: |
          dotnet restore "SaveBridge\SaveBridge\SaveBridge.csproj" `
            -r win10-x64
          Write-Host "Restore exit: $LASTEXITCODE"

      # Build and produce the APPX bundle (unsigned first)
      - name: Build APPX (x64)
        shell: powershell
        run: |
          $msbuild = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" `
            -latest -requires Microsoft.Component.MSBuild `
            -find MSBuild\**\Bin\MSBuild.exe
          if (-not $msbuild) { $msbuild = "msbuild" }
          Write-Host "Using MSBuild: $msbuild"

          & $msbuild "SaveBridge\SaveBridge\SaveBridge.csproj" `
            /p:Configuration=Release `
            /p:Platform=x64 `
            /p:RuntimeIdentifier=win10-x64 `
            /p:AppxBundle=Always `
            /p:AppxBundlePlatforms="x64" `
            /p:AppxPackageSigningEnabled=false `
            /p:UapAppxPackageBuildMode=SideloadOnly `
            /p:GenerateAppInstallerFile=false `
            /t:Build `
            /m `
            /v:normal

          Write-Host "Build exit: $LASTEXITCODE"
          exit $LASTEXITCODE

      # Sign the bundle with a self-signed developer certificate
      - name: Sign APPX bundle
        if: success()
        shell: powershell
        id: sign_appx
        run: |
          $bundle = Get-ChildItem -Recurse -Filter "*.msixbundle" | Select-Object -First 1
          if (-not $bundle) { $bundle = Get-ChildItem -Recurse -Filter "*.appxbundle" | Select-Object -First 1 }
          if (-not $bundle) { Write-Error "No bundle found"; exit 1 }
          Write-Host "Bundle: $($bundle.FullName)"

          $cert = New-SelfSignedCertificate `
            -Type Custom `
            -Subject "CN=Adoptsomekids" `
            -KeyUsage DigitalSignature `
            -FriendlyName "SaveBridge Dev Cert" `
            -CertStoreLocation "Cert:\CurrentUser\My" `
            -TextExtension @("2.5.29.37={text}1.3.6.1.5.5.7.3.3", "2.5.29.19={text}")
          Write-Host "Cert thumbprint: $($cert.Thumbprint)"

          $cerPath = "$env:GITHUB_WORKSPACE\SaveBridge-DevCert.cer"
          Export-Certificate -Cert $cert -FilePath $cerPath | Out-Null
          Write-Host "Exported cert: $cerPath"

          $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin" -Recurse -Filter "signtool.exe" |
            Where-Object { $_.FullName -like "*x64*" } | Select-Object -First 1 | ForEach-Object { $_.FullName }
          Write-Host "signtool: $signtool"

          & $signtool sign /fd SHA256 /sha1 $cert.Thumbprint /tr http://timestamp.digicert.com /td sha256 "$($bundle.FullName)"
          if ($LASTEXITCODE -ne 0) {
            & $signtool sign /fd SHA256 /sha1 $cert.Thumbprint "$($bundle.FullName)"
          }
          exit 0

      - name: Find APPX output
        if: success()
        shell: powershell
        id: find_appx
        run: |
          $appx = Get-ChildItem -Recurse -Filter "*.msixbundle" | Select-Object -First 1
          if (-not $appx) { $appx = Get-ChildItem -Recurse -Filter "*.appxbundle" | Select-Object -First 1 }
          if ($appx) {
            Write-Host "Found: $($appx.FullName)"
            echo "appx_path=$($appx.FullName)" >> $env:GITHUB_OUTPUT
            echo "appx_name=$($appx.Name)"     >> $env:GITHUB_OUTPUT
          } else {
            Write-Host "No bundle. Listing outputs:"
            Get-ChildItem -Recurse -Include "*.appx","*.appxbundle","*.msix","*.msixbundle"
          }

      - name: Upload APPX artifact
        if: success() && steps.find_appx.outputs.appx_path != ''
        uses: actions/upload-artifact@v4
        with:
          name: SaveBridge-APPX
          path: |
            ${{ steps.find_appx.outputs.appx_path }}
            ${{ github.workspace }}/SaveBridge-DevCert.cer
          retention-days: 30

      - name: Upload to Release
        if: github.event_name == 'release' && steps.find_appx.outputs.appx_path != ''
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.find_appx.outputs.appx_path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
